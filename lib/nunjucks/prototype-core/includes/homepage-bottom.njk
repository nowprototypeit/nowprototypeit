<p class="{{ nowPrototypeItParagraphAdditionalClasses }}">You can use the <a href="/manage-prototype" class="nowprototypeit-focus-link {{ nowPrototypeItLinkAdditionalClasses }}">management pages</a> to:</p>

<ul class="nowprototypeit-bullet-list {{ nowPrototypeItListAdditionalClasses }}">
  <li>
    Install, uninstall, and update plugins in <a href="/manage-prototype/plugins" class="nowprototypeit-focus-link {{ nowPrototypeItLinkAdditionalClasses }}">the plugin pages</a>
  </li>
  <li>
    Create a new page <a href="/manage-prototype/templates" class="nowprototypeit-focus-link {{ nowPrototypeItLinkAdditionalClasses }}">by using a template</a>
  </li>
  <li>
    <a href="/manage-prototype/settings" class="nowprototypeit-focus-link {{ nowPrototypeItLinkAdditionalClasses }}">Update settings</a> and more
  </li>
</ul>

{% if isRunningFromManagePrototype %}
<p class="{{ nowPrototypeItParagraphAdditionalClasses }}">You can replace this page with your own by adding an "<strong>index.{{ defaultViewExtension }}</strong>" file in "<strong>app/views</strong>", you can also do this
  <a href="/manage-prototype/templates" class="nowprototypeit-focus-link" {{ nowPrototypeItLinkAdditionalClasses }}>by using a template</a> and using the path "<strong>/index</strong>".</p>
{% else %}
  <p class="{{ nowPrototypeItParagraphAdditionalClasses }}">You can update this page, it's in your prototype kit at <strong>app/views/index.njk</strong> or <strong>app/views/index.html</strong>.</p>
{% endif %}
{% macro showPage(page) %}
  <li class="nowprototypeit-page-item"><a href="{{ page.url }}" class="nowprototypeit-page-link {{ nowPrototypeItLinkAdditionalClasses }}"
    >{{ page.name }}</a>{% if page.children %}{{ showPages(page.children) }}{% endif %}</li>
{% endmacro %}

{% macro showPages(pages) %}
  {% if pages.length > 0 %}
    <ul class="nowprototypeit-bullet-list">
      {% for page in pages %}
        {{ showPage(page) }}
      {% endfor %}
    </ul>
  {% endif %}
{% endmacro %}
{% if foundPages | length > 0 %}
  <h2 class="{{ nowPrototypeItSubheadingAdditionalClasses }}">You have the following pages in your prototype:</h2>
  {{ showPages(foundPages) }}
{% endif %}
